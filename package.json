{
  "name": "socket-clock",
  "version": "1.0.0",
  "description": "Using a websocket to emit timestampts to a frontend client",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Websocket",
    "Clock"
  ],
  "author": "Makavura Mughanga",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^3.1.1"
  }
}
